/// A class that defines the type of validation to be performed on a FormCraftTextField widget.
enum FormCraftValidationType {
  /// Every field is validated when input value changes.
  always,

  /// All fields are validated when the form is submitted.
  onSubmit,
}
